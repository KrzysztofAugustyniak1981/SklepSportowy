/* product quantity */
let quantityProduct = 1;

/* Products */
let product1 = {
    product: "Piłka nożna",
    type: "outside",
    price: 98.00,
    picture: 'src="./Images/FootballBall.jpg"'
};

/* inputs */
let productList = document.getElementById("Products");

/* get element */
let wszystko = document.querySelector(".wszystko");

/* add listner */
wszystko.addEventListener("click", wszystko())

/* Function */
function wszystko() {
    if (i=0, i<quantityProduct) {
        i++ ;
        
    }
}